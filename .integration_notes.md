# Integration phase state (from pasted_content.txt task)

## Study (Phase 1) DONE
- Notes: docs/study/01_kai.md, 02_droidmind_browseruse.md, 03_rest_licenses.md
- Licenses: Kai Apache-2.0, DroidMind Apache-2.0, Browser-Use MIT, AnythingLLM MIT, OpenHands MIT, n8n Sustainable Use mixed (adapter only), LangGraph MIT, Aider Apache-2.0, Dify Modified Apache-2.0 (API only)

## Phase 2 DONE (Kai) — compiles
- core:agent/.../backends/DeviceBackend.kt (DeviceBackend interface: id/name/capabilities/isAvailable/diagnostics + DeviceBackendRegistry with suspend select/diagnostics)
- core:agent/.../HeartbeatAgent.kt (HeartbeatEvent sealed: Silent/NeedsAttention(reason,severity); Reviewer fun interface; intervalMs default 30min; activeHoursStart=8,end=22; start(review)/stop/lastKnownEvent; onEvent listeners)
- core:network/.../providers/LocalInferenceAdapter.kt (LocalInferenceRuntime interface isSupported/availableModels/healthCheck; DisabledLocalInference object)
- core:memory/.../MemoryRefiner.kt (works on MemoryEntity; hitCount via metadata {"hits":N}; setHitCount; promotable >=5; pruneable stale+0hits+!pinned; buildSystemPromptSection)
- core:memory build.gradle.kts added libs.kotlinx.serialization.json

## Phase 3 DONE (DroidMind) — compiles
- tool:android/.../AdbDeviceBackend.kt implements DeviceBackend (id="adb"; capabilities: device.shell/apps/files/diagnostics/ui; listDevices, deviceProp, reboot, installApk, uninstall, startActivity, forceStop, listPackages, shell (allowlist), push/pull, recentLogcat, screenshot, tap, key, swipe; allow-list DEFAULT_ALLOWED; ADB optional)
- tool:android/.../AccessibilityDeviceBackend.kt (id="accessibility"; capabilities ui.tree/tap/text/scroll/key/read; uses AccessibilityAgentService.current())
- AccessibilityAgentService: added companion object current() static accessor + onCreate/onDestroy lifecycle; latestSnapshot returns Snapshot(version, root); captureTree(): Snapshot
- tool/android build.gradle.kts: added implementation(project(":core:agent"))

## Phase 4 (Browser-Use) — DONE, compiles
- feature:browser/.../BrowserBackend.kt (interface + BrowserState/BrowserExpectation/RemoteBrowserTask data classes; verify() concept implemented)
- feature:browser/.../EmbeddedWebViewBackend.kt implements BrowserBackend over real WebViewEngine API: navigate(url): Result<Unit>, snapshot(): String (accessible tree), clickSelector(sel): Boolean, typeIntoSelector(sel,text): Boolean, currentUrl(): String? (sync). submitForm = Enter on focused input; verify via url + accessibleTree.
- feature:browser/.../BrowserUseRemote.kt (REST: POST /api/v4/runs with X-Browser-Use-API-Key; poll GET /api/v4/runs/{id}; opt-in, baseUrlProvider defaults null)
- feature:browser build.gradle.kts: added libs.squareup.okhttp + libs.squareup.okhttp.logging

## Phase 5 DONE, all compile
- core:memory/.../RagComponents.kt: EmbeddingsProvider iface, KeywordEmbedder(dim=256, TF offline), VectorStore iface, InMemoryVectorStore(cosine), DocumentIngestor(chunk 800/overlap 100 + secret-scan reject), Citation data class. (serialization runtime 1.7.1 incompatible w/ Kotlin 1.9.24 — downgraded to 1.6.3 in libs.versions.toml; drop @Serializable)
- core:agent/backends/CodingBackend.kt: CodingBackend iface (id/name/capabilities/isAvailable/run(task,context): CodingResult) + CodingResult/CodingPayload
- tool:terminal/.../CodingBackends.kt: LocalSandboxCoding(TerminalToolSet.executeCommand steps from context["steps_json"]) + OpenHandsRemote (POST /api/runs Bearer token, poll /api/runs/{id}, 10min timeout)
- core:agent/.../GraphAgentEngine.kt: GraphAgentEngine<S>(entryNode, transition: NextStep) with GraphNextStep{Goto,End}, GraphCheckpoint, GraphExecutionTrace, interruptBefore, persist(), resume(). NOTE: nested data classes can't reference outer S — prefix Graph.*
- tool:filesystem/.../AiderStyleTools.kt: RepoMapTool(filesystem.repo_map, READ, maxFiles=100), FileEditTool(filesystem.edit, MODIFY, replace/append w/ find-required verification), GitDiffTool(filesystem.git_diff, READ, ProcessBuilder git)
- tool:http/.../N8nAdapter.kt: triggerWorkflowTool (webhookUrl+payload POST), latestExecution(workflowId) GET /api/v1/executions X-N8N-API-KEY; RiskLevel.COMMUNICATION
- core:network/.../providers/DifyRemoteBackend.kt: models(dify_workflow), generate(request) → flow of GenerationCompleted via POST /v1/chat-messages blocking mode; ProviderError.ProviderError_(statusCode=0,...). Added libs.kotlinx.serialization.json to core:network
- tool:terminal build.gradle: added core:agent + squareup.okhttp
- core:network build.gradle: added kotlinx.serialization.json
## After implementation
- tests: DeviceBackendRegistry, MemoryRefiner, HeartbeatAgent, InMemoryVectorIndex, N8nAdapter, BrowserUseRemote (request building), RepoMapTool.
- Build: ./gradlew assembleDebug --no-daemon -Dorg.gradle.jvmargs="-Xmx1500m" ; release with RELEASE_KEYSTORE env vars (see .hardening_notes.md).
- Version 1.1.0 / versionCode 65. Push + gh release v1.1.0 + Arabic report.

## Build reminders
- gh CLI already authed. Token placeholder in files; real token must not be committed.
- Tests currently: core/common SecretSanitizerTest+ModelsTest, core/agent ProviderRegistryTest (25 tests).

## State checkpoint (Phase 5 wiring)
- AegisApp.kt edited: added imports + fields (deviceBackendRegistry, heartbeatAgent, memoryRefiner, documentIngestor, codingBackend, graphAgentEngine); created InMemoryVectorStore/KeywordEmbedder/MemoryRefiner/DocumentIngestor/HeartbeatAgent; DeviceBackendRegistry(listOf(DeviceBackend{AccessibilityDeviceBackend()}, DeviceBackend{AdbDeviceBackend()})); LocalSandboxCoding(); GraphAgentEngine(entryNode="plan") with interruptBefore={"review"}; AgentRuntime now gets approvalPolicyProvider = { settings.getString("approval_policy") ?: "ask" }
- MUST VERIFY ctor signatures before compile: DeviceBackendRegistry(accepts List<DeviceBackend> or () -> DeviceBackend?), HeartbeatAgent(), MemoryRefiner(memoryStore), AgentRuntime(approvalPolicyProvider param name + signature)
- app build.gradle.kts must depend on :tool:android, :tool:terminal, :core:memory, :feature:browser (check existing)
- Next: compile app; add tests for: DocumentIngestor/KeywordEmbedder/InMemoryVectorStore, GraphAgentEngine (interrupt+resume), DeviceBackendRegistry, MemoryRefiner, LocalSandboxCoding(naive), CodingBackend models
- Then full build (debug+release), tests, git commit+push, release v1.1.0 (versionCode 65, versionName 1.1.0 — MUST UPDATE app/build.gradle.kts), Arabic report
- Study notes exist in docs/study/01_kai.md, 02_droidmind_browseruse.md, 03_rest_licenses.md
