# HARDENING TASK STATE (updated)

Directive file: /home/ubuntu/upload/aegis_ai_agent_full_audit_hardening_prompt_ar.txt (1547 lines)
Full task list: /home/ubuntu/CleanAgent/docs/hardening_todo.md
Audit report: /home/ubuntu/CleanAgent/docs/current-audit.md (written, complete)

## Build environment (proven working)
- Build: `cd /home/ubuntu/CleanAgent && ./gradlew <task> --no-daemon -Dorg.gradle.jvmargs="-Xmx1500m"`
- Release keystore: /home/ubuntu/aegis-release.jks pass aegis123 alias aegis; env: RELEASE_KEYSTORE=/home/ubuntu/aegis-release.jks RELEASE_KEYSTORE_PASSWORD=aegis123 RELEASE_KEY_ALIAS=aegis RELEASE_KEY_PASSWORD=aegis123
- app/build.gradle.kts: signingConfigs release uses env vars (must export before assembleRelease)
- gradle.properties has android.lintVital.enabled=false (lintVitalAnalyzeRelease kills daemon OOM)
- GH_TOKEN (NEVER commit — secret scan blocks): [REDACTED-PAT]
- Repo: https://github.com/Mtzallqmy/Ai , release v1.0.0 exists

## PROGRESS UPDATE (Phase 5 ongoing)
ADDITIONAL DONE:
- ProviderPresets.kt (12 presets) COMPILES OK
- WebViewEngine.kt REWRITTEN + compiles: JSON-encoded payloads (buildPayloadJson buildJsonObject), nav policy blocks javascript:/file:/intent:/tel:/sms:/mailto (only http/https/about allowed), safe browsing on, mixedContent NEVER_ALLOW, allowFileAccess=false, cookies third-party not needed, bridge exposes only provideSnapshot, click/type/scroll/back/forward/reload/findOnPage APIs, static IIFE JS templates ((template)(JSONpayload))
- AccessibilityAgentService.kt: scrollUp bug FIXED (ACTION_SCROLL_BACKWARD), versioned Snapshot(val version: Long, val root: UiNode?) — captureTree() now returns Snapshot (no callers break), onAccessibilityEvent refreshes latestSnapshot
- MemoryStore.put: SecretSanitizer.containsSecret check added (reject with IllegalArgumentException); core:memory build.gradle now depends on core:common
- SecretSanitizer: added containsSecret() + Bearer/credential-hint patterns
- todo items marked: docs/current-audit, AgentRuntime, ApprovalEngine, Tool schemas, ToolRuntime, CredentialVault, Key Pool, Providers presets, WebViewEngine safe JS, Screenshot observation, scrollUp fix

## REMAINING HIGH-VALUE (pick ~8 before build+release)
1. [ ] SubAgents: core:agent SubAgentRunner (delegated run, tool allowlist, budget) — ~120 lines
2. [ ] Workflows: core:workflow WorkflowEngine (steps: Agent/Tool/Condition/Loop/Delay/Approval/Output + JSON persistence) — ~200 lines; add module to settings.gradle.kts
3. [ ] Scheduling: feature:schedule WorkManager real usage (one-time + periodic sync job with constraints; honest docs) — needs to add workmanager dep to a module that has it (search gradle.properties for workmanager lib; if not present add libs.versions entry: workmanager = "2.9.1")
4. [ ] core:designsystem: AegisTheme(colors teal, Typography, Shapes, Spacing, Elevation, Motion + AegisCard, PrimaryButton, RiskChip) — ~250 lines; add module core:designsystem to settings
5. [ ] sealed ChatItem in feature:chat ChatViewModel (UserMessage/AssistantMessage/ToolCallCard/ToolResultCard/ApprovalCard/ErrorCard/SystemEvent)
6. [ ] Provider capability discovery: modelMap per provider with capabilities (streaming/tools/contextWindow) in providers; honest that listModels() server list lacks static caps — local map
7. [ ] Skills: add version/requiredCapabilities/risk to SkillDescriptor (Skills.kt exists)
8. [ ] TerminalBackend abstraction (TerminalToolSet.kt already wraps AppProcess? check; document Shizuku/Termux as documented stubs)
9. [ ] Tests: AgentRuntimeTest, ToolSchemaValidatorTest, ApprovalEngineTest, CredentialVaultTest (mock KeystoreGateway), SecretSanitizer containsSecret tests
10. [ ] CI build.yml fix (no -Plint=false; maybe keep lint disabled properly), version bump 65, release v1.0.1/1.1.0 with APK+checksums+changelog

## BUILD/RELEASE FACTS (unchanged)
- build: ./gradlew <task> --no-daemon -Dorg.gradle.jvmargs="-Xmx1500m"
- release keystore: /home/ubuntu/aegis-release.jks pass/alias aegis123/aegis; env vars: RELEASE_KEYSTORE RELEASE_KEYSTORE_PASSWORD RELEASE_KEY_ALIAS RELEASE_KEY_PASSWORD
- gh release v1.0.0 exists; GH_TOKEN in .hardening_notes header

## DONE so far (Phase 3-4)
1. core:tools ToolSchemaValidator.kt — NEW. JSON parse + schema validate + size/depth/secret-scan. ValidationResult.Valid/Invalid.
2. core:tools ApprovalEngine.kt — REWRITTEN. ASK policies now return ASK (fail-closed). requestApproval(request) suspends until respond() from UI. Channel `requests`. allowRiskLevel/alwaysAllow/denyRule. ApprovalOption now includes ASK (Models.kt updated).
3. core:model Models.kt — added DEGRADED to CapabilityAvailabilityState; ASK to ApprovalOption; artifacts/metadata to ToolResultEnvelope; new ApprovalRequestDetails.
4. core:tools ToolRuntime.kt — REWRITTEN. Schema-validated typed input (String args rejected unless valid JSON → Invalid returns failure envelope), approval route: ASK → engine.requestApproval suspend, retries with exp backoff, metadata output, capability DEGRADED allowed.
5. core:agent AgentRuntime.kt — REWRITTEN. ctor now takes approvalEngine param (wired in AegisApp). Real WAITING_FOR_APPROVAL suspension, token budget enforcement (95% threshold → finalize), maxSteps exceeded → FAILED state, retries per step, pause()/resume(), runPersistence hook, run Flow persisted, mapError typed.
6. app AegisApp.kt — wired approvalEngine into AgentRuntime ctor.
7. app FeatureScreens.kt — DEGRADED branch added.
8. core:security CredentialVault.kt — REWRITTEN. Per-(scope,name) derived sub-keys (rotation of one secret never breaks others); StrongBox detection (detectStrongBox via reflection, graceful); quarantine corrupted ciphertext (.corrupt entries); ApiKeyPool enhanced (enable/disable, updateHealth, healthSummary, usableEntries excludes rate-limited).
9. provider/openai-compatible ProviderPresets.kt — NEW. 12 presets: groq, deepSeek, xai, mistral, nvidiaNim, huggingFace, together, fireworks, cerebras, ollama, lmStudio, llamaCppServer. authHeaderValueProvider = { key -> "Bearer ${key.orEmpty()}" }.

## REMAINING (Phase 4-12)
- [ ] Wire some presets into AegisApp (optional, keep registry extensible — add register comments only or add ollama preset default off)
- [ ] Phase 4 continue: Provider capability discovery — ProviderRuntime capability gate + capabilityRegistry registration (Capability objects in AegisApp for accessibility, terminal, network) — models need capabilities populated per provider (listModels returns static capabilities per modelId map per provider)
- [ ] WebViewEngine hardening: JSON-encode JS injection (use Json.encodeToString on arguments), URL validation (shouldOverrideUrlLoading blocks file://, intent://, tel:// etc.), safe browsing, mixed content, APIs: find/scroll/back/forward/reload/tabs/download/upload/close
- [ ] AccessibilityAgentService: snapshot versioning, scrollUp bug (both scrollUp/scrollDown call ACTION_SCROLL_FORWARD — fix scrollUp→ACTION_SCROLL_BACKWARD), screenshot observation abstraction (interface, stub), open URI action
- [ ] Terminal: TerminalBackend abstraction (AppProcess default), TermuxBackend/ShizukuBackend/AdbBackend stubs w/ docs; note Rust infeasibility honestly (check cargo availability: `cargo --version` — if absent, document honestly)
- [ ] Memory: secret scan on put (SecretSanitizer check), TTL fields in entity (check schema first), type distinctions via namespace prefixes or MemoryType enum
- [ ] Skills: add version/requiredCapabilities/risk to SkillDescriptor, loadSkills into agent tools in AegisApp
- [ ] Workflows: new core:workflow module (WorkflowEngine with steps + persistence JSON) OR keep simple; Scheduling: feature:schedule using WorkManager (simple periodic sync job — real usage)
- [ ] SubAgents: agent:SubAgentRunner in core:agent (delegated run with tool allowlist + budget)
- [ ] core:designsystem module: AegisTheme/colors/typography/shapes/spacing/elevation/motion + basic components (PrimaryButton, AegisCard, RiskChip, AegisTopBar)
- [ ] ChatViewModel: sealed ChatItem (UserMessage/AssistantMessage/ToolCallCard/ToolResultCard/ApprovalCard/ErrorCard/SystemEvent) — new models + use in ChatScreen
- [ ] Room: enable exportSchema, add migration + test (check current schema version), persist AgentRun via runPersistence in AegisApp (databaseProvider agentRunDao)
- [ ] Tests: AgentRuntimeTest (approval deny/allow/ask suspend scenarios), ToolSchemaValidatorTest, ApprovalEngineTest, CredentialVaultTest (mock KeystoreGateway), provider SSE/resilience tests (Mokito? — no mockito dep; use plain fakes)
- [ ] CI build.yml: remove -Plint=false, add lint step (properly configured — android.lintVital.enabled=false in gradle.properties already; lint task may OOM — test on CI), bundleRelease, versionCode bump to 65, changelog in release notes, checksums (sha256sum)
- [ ] Release: gh release create v1.0.1 (or v1.1.0) with APK + AAB + checksums + changelog
- [ ] Final Arabic report update (technology audit table stays same + new capabilities)

## Key API facts
- AiProvider: providerId, name, suspend listModels(): Result<List<AiModel>>, suspend testConnection(): Result<Unit>, suspend generate(request): Flow<GenerationEvent>, suspend fetchModels
- OpenAiCompatibleProvider(providerId, name, baseUrlProvider: suspend()->String?, apiKeyProvider: suspend()->String?, authHeaderName, authHeaderValueProvider: (String?)->String, extraHeadersProvider, defaultModel)
- AgentRuntime(provider, toolRuntime, approvalEngine, maxSteps=25, maxTokensPerRun=200_000, executionTimeoutMs, maxRetriesPerStep, tokenBudgetThreshold, runPersistence)
- ToolRuntime(capabilityRegistry, approvalEngine); execute(tool, input, context, runId, agentId, maxToolCallsPerRun, maxRetries)
- ApprovalEngine: decide(request) → ApprovalDecision; requestApproval(request) suspend → ApprovalDecision; respond(requestId, option, toolName)
- ToolSchemaValidator.validate(raw: String, schemaJson: String) → Valid(JsonObject)/Invalid(errors)
- SettingsStrings: SecureSettings.getString(key)?: String
- Tests exist: core/common SecretSanitizerTest+ModelsTest, core/agent ProviderRegistryTest. gradle test task works.
